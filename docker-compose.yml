version: "3.9"
services:
  oshten:
    build: .
    container_name: oshten
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    command: ["python", "-u", "main.py"]

